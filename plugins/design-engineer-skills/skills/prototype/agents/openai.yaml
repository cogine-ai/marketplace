interface:
  display_name: "Prototype UI Variants"
  short_description: "Build distinct UI directions behind a live picker"
  default_prompt: "Use $prototype to build three genuinely different UI directions for this component."
policy:
  allow_implicit_invocation: false
